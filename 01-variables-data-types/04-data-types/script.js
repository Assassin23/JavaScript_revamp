// String
const firstName = 'Saras';

// Number
const age = 26;
const temp = 9.7;

// Boolean
const hasJob = true;

// Null
const isSimple = null;

// Undefined
let score;
const myscore = undefined;

// Symbol
const id = Symbol('id');

// BigInt
const n = 2345678456789n;

// Reference Type
const numbers = [1, 2, 4, 8];

const output = numbers;

console.log(output, typeof output);
